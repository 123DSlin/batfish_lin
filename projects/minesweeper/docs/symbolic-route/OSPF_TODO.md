# OSPF Symbolic Route TODO

Not implemented. The future adapter must reuse Batfish `OspfRoutingProcess` route conversion,
import/export policy, OSPF RIB preference, area/type semantics, and conditional next-hop reachability.
It must emit guarded stable routes and must not copy OSPF algorithms into Minesweeper SMT code.
